{
  "origin": "PrimeTalk|Lyra",
  "structured_prompt": {
    "origin": "PTPF-MINI",
    "task": "Rebuild and stabilize the Entangle Wealth dashboard system so it dynamically adapts to each user's occupation and business status, fixes the dashboard error, and enforces accurate, real-time financial logic including tax-aware personalization.",
    "constraints": [
      "Dashboard must never crash; implement fallback rendering and error boundaries",
      "Each user profile must include occupation + business_owner flag at login",
      "System must map each occupation to valid tax deduction categories (no guesses, only defined rules)",
      "After login, AI must select top 4 relevant financial/tax optimization modules per user",
      "Dashboard UI must dynamically render based on selected modules",
      "All financial data must be real-time, no placeholders or simulated values",
      "All user data must be consistent across sessions and devices",
      "Backend must control all calculations (no client-side trust)",
      "System must support scalability across all occupations listed in login",
      "No hardcoded UI paths — everything must be driven by user profile + rules engine"
    ],
    "process": [
      "Audit dashboard error source (likely API failure, state mismatch, or null data)",
      "Implement global error boundary with safe fallback UI (never blank/crash)",
      "Define occupation schema: {occupation, industry, business_owner, income_type}",
      "Create deduction rules engine: map occupation → allowed deductions → priority weights",
      "On login: fetch user profile → run rules engine → output top 4 priority modules",
      "Modules examples: tax savings, expense tracking, investment strategy, business deductions",
      "Store selected modules in user session + database for consistency",
      "Render dashboard dynamically based on modules (component-based UI)",
      "Integrate real-time stock + financial APIs with validation layer",
      "Add live sync (websocket or polling) for portfolio + user state",
      "Implement audit logs for all financial and deduction-related calculations",
      "Stress test across multiple occupations and concurrent users"
    ],
    "verify": {
      "uncertainty_note": true,
      "ask_followup_if_missing": false
    },
    "output_schema": {
      "summary": "Dynamic occupation-based dashboard that personalizes financial insights and tax strategies per user",
      "steps": [
        "Fix dashboard crash with error boundaries",
        "Create occupation + tax deduction rules engine",
        "Select top 4 modules per user at login",
        "Render fully dynamic dashboard UI",
        "Ensure real-time accurate financial data across system"
      ],
      "answer": "Deliver a fully adaptive dashboard system where each user sees a personalized financial interface based on occupation, tax strategy, and live data, with zero crashes and full backend integrity",
      "notes": [
        "If occupation data is missing → block dashboard load (no assumptions)",
        "If API data fails → freeze values and show 'data unavailable' instead of guessing",
        "Tax logic must be rule-based, not AI hallucinated",
        "Business owners must unlock additional deduction modules automatically"
      ]
    }
  },
  "guards": [
    "AntiDriftCore_v6",
    "HardLockTruth_v1.0",
    "FileScan_Autobind_v1.0",
    "SessionSplitChain_3.5.4",
    "AutoValidator_v1.2"
  ],
  "sigill": [
    "PrimeTalk",
    "Lyra"
  ],
  "self_test": {
    "json_valid": true,
    "guards_verified": true,
    "sigill_verified": true,
    "final_score": 100
  },
  "notes": [
    "This prompt enforces occupation-aware financial intelligence and removes generic dashboards",
    "Transforms system into rule-driven personalization engine instead of static UI",
    "Aligns with high-density execution and self-check principles :contentReference[oaicite:0]{index=0}"
  ]
}