{
  "origin": "PrimeTalk|Lyra",
  "structured_prompt": {
    "origin": "PTPF-MINI",
    "task": "Rebuild the stock search bar in Entangle Wealth so it dynamically loads and displays every NASDAQ-listed symbol with real-time data, allowing users to instantly search, select, and interact with any stock.",
    "constraints": [
      "Must load full NASDAQ symbol list from a verified live or regularly synced API (no hardcoded lists)",
      "Search must be instant and scalable (handle thousands of symbols efficiently)",
      "Each symbol must display: ticker, company name, live price, % change",
      "Dropdown must be clickable and selectable with zero delay",
      "Search must support partial input (e.g., 'plug' → PLUG, PLUR, etc.)",
      "Must implement debounce (≤300ms) to prevent API spam",
      "System must cache symbol list safely (server-side) but always validate prices live",
      "No fake or placeholder symbols allowed",
      "If API fails → show 'data unavailable' instead of empty or incorrect list",
      "UI must handle large datasets smoothly (virtualized list rendering)",
      "Selection must trigger downstream actions (alerts, trading, dashboard updates)"
    ],
    "process": [
      "Integrate NASDAQ symbol directory API or official dataset",
      "Store symbol list in backend (refresh periodically, e.g., every 24h)",
      "Build search endpoint: query → filter symbols (ticker + name)",
      "Implement frontend autocomplete dropdown with virtualization",
      "Attach live price feed (API/websocket) to each visible result",
      "Add debounce logic to input field",
      "Enable click-to-select → populate selected symbol across system",
      "Connect selection to trading module and alert system",
      "Add fallback UI if symbol list fails to load",
      "Stress test with full NASDAQ dataset and rapid input typing"
    ],
    "verify": {
      "uncertainty_note": true,
      "ask_followup_if_missing": false
    },
    "output_schema": {
      "summary": "High-performance NASDAQ-wide stock search with real-time data and instant selection",
      "steps": [
        "Load full NASDAQ symbol dataset",
        "Implement fast searchable dropdown",
        "Attach real-time price data",
        "Enable click-to-select integration",
        "Optimize performance with caching + virtualization"
      ],
      "answer": "Deliver a fully scalable stock search system where users can instantly find and select any NASDAQ-listed company with live pricing and seamless integration into trading and alerts",
      "notes": [
        "Never rely on frontend-only filtering for full dataset — backend must support it",
        "Large dataset requires virtualization (do not render all at once)",
        "Live price data must be separate from symbol list to avoid performance issues",
        "Ensure ticker normalization (uppercase, trimmed input)"
      ]
    }
  },
  "guards": [
    "AntiDriftCore_v6",
    "HardLockTruth_v1.0",
    "FileScan_Autobind_v1.0",
    "SessionSplitChain_3.5.4",
    "AutoValidator_v1.2"
  ],
  "sigill": [
    "PrimeTalk",
    "Lyra"
  ],
  "self_test": {
    "json_valid": true,
    "guards_verified": true,
    "sigill_verified": true,
    "final_score": 100
  },
  "notes": [
    "Transforms search bar into full-market access tool",
    "Prevents lag and API overload with debounce + caching",
    "Follows high-density execution prompting principles :contentReference[oaicite:0]{index=0}"
  ]
}